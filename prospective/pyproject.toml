[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "emina-kropff-prospective-toy"
version = "0.1.0"
description = "Mechanism-first reproduction of Emina and Kropff feedforward self-organization"
requires-python = ">=3.11"
dependencies = [
  "matplotlib",
  "numpy",
  "PyYAML",
  "scipy",
  "tqdm",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

